"""Checkpoint 4 (minimal MVP): Grok-backed hypothesis agents.

The LLM provider is isolated behind agents.grok_client so the pipeline
works with any provider (or offline fallback) without rewrites.
"""
